#!/bin/bash
case "$1" in
  edit)
    code $DOT_FILE_DIR
  ;;
  save)
    INITDIR=$PWD
    cd $DOT_FILE_DIR
    git add --all
    git commit
    git push
    cd $INITDIR
  ;;
  update)
    INITDIR=$PWD
    cd $DOT_FILE_DIR
    git pull
    git submodule update
    cd $INITDIR
  ;;
esac